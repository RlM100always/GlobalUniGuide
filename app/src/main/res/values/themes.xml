<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AllUniversityInformation" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="AppTheme.SearchView" parent="Widget.AppCompat.SearchView">
        <!-- Customize the query text color -->
        <item name="android:imeOptions">actionSearch</item>
        <item name="android:inputType">text</item>
        <item name="android:textColor">@android:color/black</item>
        <!-- Customize the hint text color -->
        <item name="android:textColorHint">@android:color/black</item>
    </style>
    <style name="MyToolbarTitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">serif</item>

    </style>

        <!-- Define a custom style for the toolbar -->
    <style name="MyPopupTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColor">#D81B60</item> <!-- Set popup menu text color -->
        <item name="android:textStyle">bold</item> <!-- Set popup menu text color -->

    </style>

    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">#373837</item>
        <item name="colorControlActivated">#EF045A</item>
    </style>

    <style name="RatingBar1" parent="Theme.AppCompat">
        <item name="colorControlNormal">#959A9A</item>
        <item name="colorControlActivated">#0227F8</item>
    </style>



    <style name="CustomProgressDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Theme.AllUniversityInformation" parent="Base.Theme.AllUniversityInformation" />
</resources>